<h1> <%= @actor["name"] %> </h1>

<table>
  <tr>
    <th>Movie Title</th>
    <th>Character</th>
  </tr>
  <% @roles.each do |role| %>
  <tr>
    <td><a href="/movies/<%= @movie_id[0]["id"] %>"> <%= role["title"] %> </a></td>
    <td> <%= role["character"] %> </td>
  <tr>
  <% end %>
<table>
